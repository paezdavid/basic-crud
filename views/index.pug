extends layout

block content
  .wrapper 
    header 
      div
        h1 Operaciones CRUD.
      nav 
        a(href="/form")  CREATE 
        a(href="/")  READ
        

    main 
      h2 READ.
      p En esta sección del sitio web, leo datos de una database para mostrarlos en el cliente. 
      h3 Cantidad total de libros: #{numberOfDocs}
      each book in books 
        ul 
          a(href=`/book/${book._id}`).bookSelector 
            li=book.bookName
